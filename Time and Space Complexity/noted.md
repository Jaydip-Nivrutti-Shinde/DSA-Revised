----- TC ------
> Information 
    - it is not hardware dependent
    - we optimizes the code to make code efficient and run fastly 
    - fast code does not means less number of lines
    - sometimes break statement works for this 
    - we can say it is number of iterations or operations 
    - O(kn) => O(n), O(kn+-m) => O(n), O(k) => O(1) constant TC
    - Linear time  , 
    - 1 > logn > root of n > n > nlogn > n^2 > n^3 >> 2^n
    - constant, logarithmic, square root, linear,-,quadratic,-, exponential TC
    - to avoid or solve Time Limit Exceeded error we optimizes our code 
    - 

----- SC & Auxillary space -----
    - extra space taken to solve problem
    - O(n), O(1)-when not used any extra space or constant space 
    - 
> Questions 
    1} find duplicate in array
        approach 1 - nested loop / brute force
        approach 2 - boolean flag array  
        approach 3 - using mathematical logic 